package com.neversoft.launcher.theme
import androidx.compose.ui.graphics.Color

// Theme-driven color tokens — always read from LocalLauncherTheme.current
// These constants are fallbacks only; do not hardcode in UI composables
object NsColors {
    val Danger   = Color(0xFFFF3B30)
    val Overlay  = Color(0x99000000)
    val Scrim    = Color(0xB5000000)
}
